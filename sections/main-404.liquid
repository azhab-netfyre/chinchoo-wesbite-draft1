
<section class="section-pad" style="padding: 5rem 0; min-height: 55vh; display:flex; align-items:center;">
  <div class="container" style="max-width: 800px; text-align:center;">
    <p style="font-size:0.9rem; letter-spacing:0.1em; text-transform:uppercase; color:hsl(var(--accent-bubblegum)); margin-bottom:1rem;">404</p>
    <h1 style="font-size:4rem; margin-bottom:1rem;">Not found</h1>
    <p style="margin-bottom:2rem;">The page you were looking for does not exist.</p>
    <a class="btn btn-primary" href="{{ routes.root_url }}">Back to {{ shop.name }}</a>
  </div>
</section>

{% schema %}
{ "name": "Main 404", "settings": [] }
{% endschema %}
